<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="horizontal" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingTop="@dimen/dp_sm"
    android:paddingBottom="@dimen/dp_sm" android:gravity="center_vertical">

    <TextView android:id="@+id/item_word_text"
        android:paddingStart="@dimen/dp_sm"
        android:paddingEnd="@dimen/dp_sm"
        android:textSize="@dimen/font_xs"
        android:layout_width="match_parent"
        android:gravity="center_vertical"
        android:layout_height="match_parent"
        android:layout_alignParentStart="true"/>

    <ImageButton android:id="@+id/item_word_button_delete"
        android:contentDescription="@string/button_delete"
        android:src="@drawable/ic_close"
        android:layout_width="wrap_content"
        android:background="@android:color/transparent"
        android:layout_height="wrap_content"
        android:layout_marginEnd="@dimen/dp_xs"
        android:layout_centerVertical="true"
        android:layout_alignParentEnd="true"/>
</RelativeLayout>
